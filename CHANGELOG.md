### 新增
- 订阅用户信息回传：当仅请求单个订阅时，通过 HTTP 响应头 `Subscription-Userinfo` 返回 `upload`、`download`、`total`、`expire` 等信息，便于客户端展示和限额同步。
- 可视化订阅信息节点：当启用配置 `ShowSubInNodes` 且订阅端返回流量/到期信息时，自动在节点列表中插入一条汇总节点，显示“剩余流量/剩余天数”。

### 变更与改进
- 解析流程增加调试日志，记录成功解析的节点数量；调整导入顺序以提升代码可读性与可维护性。